{
  "name": "deepseek-desktop",
  "version": "1.4.1",
  "description": "DeepSeek Desktop —— 内嵌 chat.deepseek.com 的桌面客户端（Electron + WebContentsView）",
  "productName": "DeepSeek",
  "main": "dist/main/main.js",
  "author": "Kou",
  "license": "MIT",
  "scripts": {
    "start": "tsc && node scripts/copy-assets.js && electron .",
    "build": "tsc && node scripts/copy-assets.js && electron-builder",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "electron": "^37.10.3",
    "electron-builder": "^24.13.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "uiohook-napi": "^1.5.5"
  }
}
