# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

> DeepSeek 网页版（chat.deepseek.com）的 Electron 桌面封装；界面外壳为本地 Web 技术（HTML/CSS/JS），主体对话内容渲染官方网页。原生桌面外壳不改变其设计语言为原生。

## Stack

Electron 31 + TypeScript（主进程）/ 原生 HTML/CSS/JS（外壳窗口）+ WebContentsView 内嵌官方网页。本项目已存在，此条目仅记录事实。

## Users

- **办公知识工作者**（已确认主要用户）：在文档、WPS、浏览器等应用中办公，需要随时向 AI 提问而不中断当前工作。
- 次要受众（未单独确认）：开发者与深度 AI 用户被默认覆盖，但未作为独立画像确认。

## Product Purpose

让「边工作边问 AI」成为零摩擦：无需在桌面与网页之间来回切换。成功 = 用户在任意应用里，用最短路径（划词、快捷键、截图、共享）完成一次 AI 问答，然后回到原任务。

## Positioning

相对直接用 DeepSeek 网页版，本客户端把四种高频动作搬进用户的当前工作流，且不需要先打开浏览器：

- **划词即用**：任何应用内选中文字即弹工具栏（复制/翻译/解释/问问 DeepSeek），无需快捷键。
- **截图识图**：快捷键或按钮唤起截图，选区后直接提取文字、翻译、解释或向 AI 提问。
- **快捷副窗口**：全局快捷键随时呼出/隐藏副窗口，主/副窗口并行，B 窗口（临时窗口）即用即走。
- **共享屏幕/文档**：发送消息时自动附带当前屏幕截图或 WPS 文档（Word/Excel/PDF）最新内容。

## Operating Context

- Windows 桌面环境（Electron 31）；常驻系统托盘，可开机自启、关闭到托盘。
- 用户在 WPS/Word、浏览器、IDE 等第三方应用中工作，AI 交互以键盘快捷键 + 划词 + 截图为主。
- 共享屏幕时四角显示指示框、任务栏显示共享按钮；共享文档依赖本地 WPS 打开对应文件。
- 未登录 DeepSeek 账号时，对话、识图等能力不可用，须在窗口内完成登录。
- 应用内置「详细使用说明」并支持把说明书提交给 AI 解读（说明书问问 AI）。

## Capabilities and Constraints

已确认能力：

- 主窗口 / 副窗口 / B 窗口（临时窗口）三类窗口，支持置顶、主副切换。
- 划词工具栏：按钮可增删、拖拽排序、自定义提示词；快捷键可全局开关。
- 截图：选区 + 标注（画笔/矩形/椭圆/颜色/撤销/清空）+ 窗口吸附；动作含复制、提取文字、翻译、解释、发送到当前/新对话。
- 共享屏幕：发送消息自动附带截图；非识图模式（快速模式）提示仅支持 OCR。
- 共享 WPS 文档：Word/Excel/PDF，自动检测改动并重提（轮数、触发阈值可配置）。
- 全局快捷键：截图（默认 左 Alt + C）、副窗口呼出（默认 左 Alt + 空格）、划词开关。
- 模型模式：快速 / 专家 / 识图；识图模式依赖账号灰度与网页端支持。
- AI 回答完成提醒（窗口不在前台时通知，点击跳回会话）。
- 设置面板：主题（浅色/深色/跟随系统）、全局字号（-10 ~ +10）、通知分类开关、提示词模板自定义（识图/提取文字/翻译/解释）、共享空闲自动退出、链接打开方式（内置浏览器/系统默认）。
- 软件内更新（GitHub Release 自动检查/下载/安装）；导出对话记录（引导至网页版导出）。

已确认约束：

- 依赖 DeepSeek 官方网页端渲染与账号；网页结构变化可能导致注入逻辑失效。
- 提示词模板占位符：`{content}`、`{targetLang}`。
- 共享屏幕/文档存在空闲自动退出（可配置，0 = 不自动退出）。
- 平台仅支持 Windows（托盘/开机自启/快捷键逻辑均面向 Windows）。

## Brand Commitments

- 产品名「DeepSeek 桌面版」，中文界面。
- **必须保留**：MIT 开源许可；与 DeepSeek 官方**无任何关联或背书**（首次运行用户须知 + LICENSE 明文声明），界面不得冒充官方产品。
- 未绑定任何视觉规范（无官方品牌视觉资产授权）。

## Evidence on Hand

- 首次运行用户须知文本（firstRun.html）为法律性声明，属必须保留的事实性内容，不得改语义。
- 无真实用户访谈、案例或测试数据；不编造用户证言。
- 无已授权的品牌视觉资产。

## Product Principles

1. **不打断工作流**：划词、快捷键、截图、共享都发生在用户当前所在的上下文里，核心交互不得强迫用户先回到主窗口。
2. **一步呼出，两步完成**：任何常用动作应可用 ≤1 次呼出 + ≤1 次提交完成；呼出必须可逆（快捷键再按即隐藏）。
3. **自解释**：每项设置带说明（hint），内置说明书，说明书本身可交给 AI 解读；状态/错误提示说清「发生了什么、为什么、怎么办」。
4. **本地优先与透明**：数据本地存储、开源、免责声明明确；用户可控制通知、自启、主题、字号等全部偏好。
5. **可定制性**：提示词、划词按钮、快捷键、字号、主题均为用户可配置项，默认值面向办公场景设计。

## Accessibility & Inclusion

- 已确认：全局字号可调（-10 ~ +10）；浅色/深色/跟随系统主题；纯键盘快捷键操作路径。
- 未确认：具体无障碍标准承诺（如 WCAG 级别）；未建立产品级无障碍测试流程。
